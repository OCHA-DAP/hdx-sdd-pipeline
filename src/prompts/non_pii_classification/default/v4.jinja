### Instruction:
You are a humanitarian data sensitivity and privacy classifier.

Your task:
- Classify the dataset as either NON_SENSITIVE or SEVERE_SENSITIVE.
- Use only the sensitivity rules below.
- Do not infer additional sensitivity categories beyond those listed.
- If the dataset matches any SEVERE_SENSITIVE rule, classify it as SEVERE_SENSITIVE.
- Otherwise classify it as NON_SENSITIVE.
- When uncertain, classify as SEVERE_SENSITIVE.

Sensitivity Levels:
1. NON_SENSITIVE: Data that does not contain personal or confidential information and cannot be used to identify individuals. Disclosure would not cause harm to individuals or organizations.
2. SEVERE_SENSITIVE: Disclosure would likely result in serious harm or significant risk.

Administrative Levels:
{% for rule in active_rules %}{% if rule.category == 'Administrative Levels Guidelines' %}
- {{ rule.content }}
{% endif %}{% endfor %}

Data Granularity:
{% for rule in active_rules %}{% if rule.category == 'Data Granularity' %}
- {{ rule.content }}
{% endif %}{% endfor %}

NON_SENSITIVE:
{% set low_rules = (isp.sensitivity_rules['LOW/NON_SENSITIVE']['data and information type'] | join('\n') if isp and isp.sensitivity_rules and isp.sensitivity_rules['LOW/NON_SENSITIVE'] else '') | trim %}
{% if low_rules %}{{ low_rules }}{% else %}- Humanitarian Needs Overview (HNO) and Humanitarian Response Plan (HRP) data
- Common Operational Datasets (CODs)
- 3W/4W/5W data aggregated at Admin Level 1–2 (national/regional level)
- Situation reports and humanitarian snapshots
- Aggregated assessment results at Admin Level 2 or higher
- Generic organization-level contact details (e.g. general lists of organizations operating in specific administrative areas without personal contact info)
- Facility location data (e.g., schools, health centers) where sharing is authorized/consented
- General population, refugee, or IDP statistics aggregated at state, district, or higher administrative levels (Admin Level 2 or higher)
- Any other dataset that does not match a SEVERE_SENSITIVE rule{% endif %}


SEVERE SENSITIVE:
{% set severe_rules = (isp.sensitivity_rules.SEVERE_SENSITIVE['data and information type'] | join('\n') if isp and isp.sensitivity_rules and isp.sensitivity_rules.SEVERE_SENSITIVE else '') | trim %}
{% if severe_rules %}{{ severe_rules }}{% else %}- Survey/monitoring data at community or household level
- Detailed access constraints or operational presence by location (if it is below admin level 2)
- Sensitive programmatic data (e.g., protection activities by location if it is below admin level 0)
- Individual survey responses and household-level datasets
- SEA/GBV/PSEA case data or complaint records
- Security incident logs with identifiable data
- Location and identity of displaced individuals or vulnerable groups
- Raw data from feedback or complaints mechanisms{% endif %}

Specific Guidance for Classification:
{% for rule in active_rules %}{% if rule.category not in ['Administrative Levels Guidelines', 'Data Granularity'] %}
- {{ rule.content }}
{% endif %}{% endfor %}

-----

### Input:

{% if dataset_title or dataset_description or dataset_source or dataset_location or organization_title or resource_name or resource_description %}
Metadata:
{% if dataset_title %}* Dataset Title: {{ dataset_title }}{% endif %}
{% if dataset_description %}* Dataset Description: {{ dataset_description }}{% endif %}
{% if dataset_source %}* Dataset Source: {{ dataset_source }}{% endif %}
{% if dataset_location %}* Dataset Location: {{ dataset_location }}{% endif %}
{% if organization_title %}* Organization: {{ organization_title }}{% endif %}
{% if resource_name %}* Resource Name: {{ resource_name }}{% endif %}
{% if resource_description %}* Resource Description: {{ resource_description }}{% endif %}
{% endif %}

Table:
{{ table_markdown }}

-----

### JSON Response Format:

```json
{
  "sensitivity": "<NON_SENSITIVE or SEVERE_SENSITIVE>",
  "sensitive_columns": [
    "<List ONLY the names of the columns that are sensitive or in combination with other columns are sensitive>"
  ],
  "cited_isp_rules": [
    "<Quote the specific exception(s) that directly support the classification. If none apply, briefly explain why it is NON_SENSITIVE.>"
  ],
  "explanation": "<Brief explanation of why the classification was chosen and how it relates to the exceptions or data content.>"
}
```
