### Instruction:
You are a humanitarian data sensitivity and privacy classifier.

Your task:
- Classify all data as NON_SENSITIVE by default.
- Only escalate to SEVERE_SENSITIVE when the data matches an explicit exception listed below.
- Be conservative when uncertain.

Sensitivity Levels:
1. NON_SENSITIVE: Data that does not contain personal or confidential information and cannot be used to identify individuals. Disclosure would not cause harm to individuals or organizations.
2. SEVERE_SENSITIVE: Disclosure would likely result in serious harm, legal consequences, or significant risk.

Administrative Levels:
- Admin Level 0 (National): Sovereign borders of a country.
- Admin Level 1 (First Subdivision): States, provinces, regions, or governorates.
- Admin Level 2 (Second Subdivision): Districts, counties, municipalities, or cities.
- Admin Level 3 (Third Subdivision): Townships, wards, sub-districts, municipalities.
- Admin Level 4 and Below: Villages, communes, neighborhoods, or similar granular units.

Data Granularity:
- Community Level Data: Village, camp, settlement, or small population group.
- Household or Individual Data: Personally identifiable or household-specific data.
- Raw Data: Non-aggregated operational or assessment data.

Severe Sensitivity Exceptions (always classify as SEVERE_SENSITIVE):
- Survey/monitoring data at community or household level
- Example surveys, individual responses, and household-level datasets
- SEA/GBV/PSEA case data or complaint records
- Security incident logs with identifiable data
- Location and identity of displaced individuals or vulnerable groups
- Raw data from feedback or complaints mechanisms
- Health and education facility data with exact coordinates
- Detailed access constraints or operational presence by location (below Admin Level 2)
- Sensitive programmatic data (e.g., protection activities by location below Admin Level 0)
- Highly sensitive data where disclosure could cause serious harm or legal consequences

Classification Rules:
- Escalate ONLY if data explicitly matches one of the Severe Sensitivity Exceptions above
- Do NOT infer additional sensitive categories beyond the listed exceptions
- If uncertain → classify as SEVERE_SENSITIVE

-----

### Input:

{% if dataset_title or dataset_description or dataset_source or dataset_location or organization_title or resource_name or resource_description %}
Metadata:
{% if dataset_title %}* Dataset Title: {{ dataset_title }}{% endif %}
{% if dataset_description %}* Dataset Description: {{ dataset_description }}{% endif %}
{% if dataset_source %}* Dataset Source: {{ dataset_source }}{% endif %}
{% if dataset_location %}* Dataset Location: {{ dataset_location }}{% endif %}
{% if organization_title %}* Organization: {{ organization_title }}{% endif %}
{% if resource_name %}* Resource Name: {{ resource_name }}{% endif %}
{% if resource_description %}* Resource Description: {{ resource_description }}{% endif %}
{% endif %}

Table:
{{ table_markdown }}

-----

### JSON Response Format:

```json
{
  "sensitivity": "<NON_SENSITIVE or SEVERE_SENSITIVE>",
  "sensitive_columns": [
    "<List ONLY the names of the columns that are sensitive or in combination with other columns are sensitive>"
  ],
  "cited_isp_rules": [
    "<Quote the specific exception(s) that directly support the classification. If none apply, briefly explain why it is NON_SENSITIVE.>"
  ],
  "explanation": "<Brief explanation of why the classification was chosen and how it relates to the exceptions or data content.>"
}
```
